import { type NextPage } from "next";
import Head from "next/head";

import { signIn, signOut, useSession } from "next-auth/react";

import { useRouter } from "next/router";
import { useEffect } from "react";
const Home: NextPage = () => {
  const { data: session, status } = useSession();
  const router = useRouter();
  const handleSignIn = async () => {
    await signIn();
  };

  useEffect(() => {
    console.log(status);
    if (session?.user) {
      void router.push("/home");
    }
  }, [session]);
  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className="flex h-screen w-full flex-col items-center justify-center gap-4 px-2 sm:px-8">
        <h1 className="text-center text-3xl font-bold">GitHub Task Manager</h1>
        <h2 className="max-w-sm text-center text-current opacity-70">
          Simplify your GitHub issue management with GitHub Issue Manager. Sign
          in now and start getting things done.
        </h2>
        <button className="btn" onClick={handleSignIn}>
          sign in with GitHub
        </button>
      </div>
    </>
  );
};

export default Home;
